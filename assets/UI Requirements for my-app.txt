UI Requirements for my-app
Alright Damson — here is the **full, thorough, production-level UI prompt** you requested.
This is the one you’ll give to ANY AI when you want it to generate **frontend UI code** for your Caprianne Trdz system with:

✔ A **TradeZella-like, cinematic interface**
✔ Adaptive UI for **mobile, desktop, and web**
✔ A **calendar heatmap** (green = profitable, red = losing)
✔ Theme-aware design (dark/light)
✔ Respect for **platform layout rules**
✔ The structure and logic from your trading system document

This prompt is **complete, structured, unambiguous**, and will give any AI everything it needs to generate UI components, screens, layouts, and styling for your app.

---

# ⭐ **THE THOROUGH UI PROMPT (Copy & Use Anywhere)**

> **You are building the UI for a cross-platform trading performance system named Caprianne Trdz. The frontend must be created using a single React codebase with Expo (React Native for mobile), React Native Web (for web), and Tauri (wrapping the web build for desktop). The UI must automatically adapt to mobile, desktop, and web device architecture and sizing rules.**
>
> ---
>
> # 🌑 **THEME REQUIREMENTS**
>
> The UI must follow a **TradeZella-inspired cinematic aesthetic**:
>
> ### **Dark Theme**
>
> * Background: `#0d0d0d` (graphite)
> * Text: `#f5f5f5`
> * Highlight: `#00d4d4` (cyan glow)
> * Profit: gradient greens (`#1f7a1f` → `#4caf50`)
> * Loss: gradient reds (`#b71c1c` → `#ef5350`)
>
> ### **Light Theme**
>
> * Background: `#f7f7f7`
> * Text: `#101010`
> * Highlight: `#0099a8`
> * Profit: light greens
> * Loss: light reds
>
> The **calendar heatmap** and all UI components must adapt to active theme colors.
>
> All animations must use **React Native Reanimated** or **Framer Motion** (for web).
>
> ---
>
> # 🔥 **CORE UI RULES**
>
> ### 1. **Mobile UI Rules**
>
> * Stacked, vertical layouts
> * Swipe gestures
> * Bottom tab navigation
> * Cards with large touch targets
> * Collapsible sections
>
> ### 2. **Web UI Rules**
>
> * Wider layouts
> * Side navigation bar
> * More whitespace
> * Multi-column grids where appropriate
>
> ### 3. **Desktop (Tauri) UI Rules**
>
> * Windowed experience
> * Responsive resizing
> * Sidebar navigation
> * Keyboard-friendly interactions
> * Charts expand to full width
>
> The UI MUST adapt seamlessly depending on screen width.
>
> ---
>
> # 📅 **CALENDAR HEATMAP REQUIREMENTS**
>
> Build a TradeZella-style trading calendar:
>
> * Each day is a square block
> * **Profit = green block** (darkness = profitability)
> * **Loss = red block** (darkness = size of loss)
> * Break-even = gray
> * No trades = neutral color
>
> ### Required features:
>
> * Tap/click on a day → open that day’s trades
> * Hover on desktop → show tooltip with summary
> * Calendar respects device UI rules (mobile vertical → desktop horizontal)
> * Calendar respects active theme
> * Entire calendar month updates based on Firebase trades
>
> ---
>
> # 📊 **DASHBOARD UI REQUIREMENTS**
>
> The dashboard must include:
>
> * Equity curve chart
> * Calendar heatmap
> * Today’s emotional rating
> * Profit/loss summary
> * Confluence score averages
> * A+, A, B, C, D trade distribution
> * Quick buttons for “Add Trade” and “Journal”
>
> Style = clean, smooth, minimal, futuristic (TradeZella energy).
>
> ---
>
> # 📘 **JOURNAL UI REQUIREMENTS**
>
> A journal screen with:
>
> * Scrollable list of trade cards
> * Each card shows:
>
>   * Pair
>   * R:R
>   * Result color (green/red/gray)
>   * Date
>   * Setup type
> * Filter menu (pair, session, setup, date, score, win/loss)
> * Tapping a card → opens full trade detail screen
>
> ---
>
> # 📝 **TRADE DETAIL SCREEN UI**
>
> Include:
>
> * Pair, direction, session
> * Entry, SL, TP, exit price
> * Auto-calculated R:R
> * Confluence score breakdown by category
> * Upload screenshot section
> * Notes area
> * Psychology tags
> * Emotional rating (1–10)
> * Rule deviation toggle
>
> Must show score visually (progress ring or gradient bar).
>
> ---
>
> # 🧮 **NEW TRADE ENTRY FORM UI**
>
> Form must include inputs for:
>
> * Pair selection
> * Direction
> * Session
> * Entry / SL / TP
> * Result (win/loss/breakeven)
> * Actual exit (optional)
> * Setup type
> * Emotional state (1–10 slider)
> * Rule deviation (checkbox)
> * Notes
>
> When Entry/SL/TP are typed:
> **Auto-calc R:R** and show it live.
>
> ---
>
> # 🧠 **PSYCHOLOGY LOG UI**
>
> A daily log screen with:
>
> * Mood selector (1–10)
> * Notes
> * Deviations
> * Intentions for the session
>
> Display psychology history in a scrollable chart or list.
>
> ---
>
> # 📋 **SMC CHECKLIST EDITOR UI**
>
> Editable table where the user can:
>
> * Add new checklist items
> * Delete items
> * Edit label
> * Edit description
> * Edit weight
> * Change category:
>
>   * Critical
>   * Important
>   * Optional
>
> Table must support:
>
> * Inline editing
> * Reordering items
> * Category color coding
>
> ---
>
> # 📁 **NAVIGATION STRUCTURE**
>
> **Mobile:**
>
> * Bottom tabs → Dashboard, Journal, Analytics, Routine, Settings
> * Stack modals for Add Trade + Trade Detail
>
> **Web/Desktop:**
>
> * Left sidebar navigation
> * Modal centered on screen for Add Trade
>
> ---
>
> # 🎨 **COMPONENT STYLING**
>
> All components must:
>
> * Use consistent spacing system (8px scale)
> * Have subtle shadows
> * Rounded corners (12–20px)
> * Glow/blur accents
> * Smooth animations
> * Support dark & light theme
>
> ---
>
> # 🔥 **WHAT THE AI MUST OUTPUT**
>
> When generating UI:
>
> * Use functional React components
> * Use Expo + React Native components
> * Use responsive design
> * Use proper styling for each platform
> * Use reusable components (Card, Section, ScreenLayout, HeatmapCalendar, TradeCard, etc.)
> * Use TypeScript
> * Respect the data models from the project
> * Follow the full UI rules above
>
> End result:
> **A modern, cinematic, cross-platform UI with a TradeZella-like experience.**
